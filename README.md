## Run Backend

1. Run `go mod tidy`
2. Run `go run main.go`
3. Backend API will be run on `http://localhost:8000`

## API Spec

API Spec is available in `BookCabin.postman_collection.json` file. Open it in Postman.
